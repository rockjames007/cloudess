import 'package:flutter/material.dart';
void main() => runApp(ContactInfoFragment());
class ContactInfoFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
        body:new Container(
          color: Colors.purpleAccent,
        )

    );
  }
}
